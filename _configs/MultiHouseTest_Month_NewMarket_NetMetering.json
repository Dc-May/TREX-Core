{
  "version": "3.7.0",
  "study": {
    "name": "EpisodeTests_net_profit_Reward", // 0bat mean: -79.56335297601616, -netload_settle: 8.129579796950543
      "description": "",
      "start_datetime": "2016-08-01 0:0:0", // do not change,
      "start_datetime_sequence": "sequential", // do not change,
      "timezone": "America/Vancouver", // do not change,
      "days": 30, //Change if we want more days, goes up to 365
      "time_step_size": 3600, // do not change, 1 hour
      "generations": 100000 //Change if we want more generations
//      "profiles_db_location": "taken from configs"
    },

  "server": {
      "host": ""
    }, // Unimportant for this one, should be taken care of by TREX-Core
  "sim_controller": {"kill_list_name": null}, //these will be edited at startup time from the gym agent

  "training": {
      "curriculum": {}
    }, // Unimportant for this one

    "market": {
      "id": "",
      "type": "MicroTE3DCM_even_simpler",
      // no batteries             no market: 0                      MicroTE3DCM_simple: 4.723780547137312,    MicroTE3DCM_even_simpler 4.646189197380778
      // Greedy (-net_set)        no market:  8.129579796950543                          10.190324238617912                            10.168231200532611
      "close_steps": 2,
      "grid": {
        "price": 0.069,
        "fee_ratio": 1.1
      }
    }, // do not change, the market will stay fixed

    "participants": {

    "Building_1": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit", "grid_equivalent"
        "use_synthetic_profile": "Building_1"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_2": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_2"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_3": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_3"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_4": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit", grid_equivalent
        "use_synthetic_profile": "Building_4"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_5": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit", grid_equivalent
        "use_synthetic_profile": "Building_5"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_6": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit", grid_equivalent
        "use_synthetic_profile": "Building_6"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_7": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_7"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_8": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_8"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_9": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_9"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_10": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_10"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_11": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_11"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_12": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_12"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_13": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_13"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_14": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_14"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_15": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_15"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_16": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_16"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    },

    "Building_17": {
      // I'd recommend keeping the name the same as the name of the synthetic profile
      "type": "Residential",
      "trader": {
        "track_metrics": true,
        "type": "gym_agent",
        "P_max": 3000,
        "actions": {
          "price_ask": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_ask": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the  max accordingly? Min need to stay at 0

          "price_bid": {
            "heuristic": "fixed",
            "max": 0.1449,
            "min": 0.069
          },
          // "learned, keep the min and max the same as the market
          "quantity_bid": {
            "heuristic": "netload",
            "max": 10,
            "min": 0
          },
          // "learned, adjust the max accordingly? Min need to stay at 0
          "storage": {
            "heuristic": "learned",
            "max": 3000,
            "min": -3000
          }
          //"learned", "greedy"
        },
        "learning": true,
"observations": [
          //"daytime_sin",
          //"daytime_cos",
          //"yeartime_sin",
          //"yeartime_cos",
          "netload_settle", //"netload_deliver", "netload_settle", "netload_now",
          "SoC_settle", //SoC, Need to check if this actyuall works!!!!

          //"avg_bid_price",
          //"total_bid_quantity",
          //"avg_ask_price",
          //"total_ask_quantity"

          //"avg_ask_quantity",       // might not be needed since market settles in expecation
          // "avg_bid_quantity",      // might not be needed since market settles in expecation
          //"std_ask_quantity",       // might not be needed since market settles in expecation
          // "std_bid_quantity",      // might not be needed since market settles in expecation
          //"total_settled_quantity", // might not be needed since it can be inferred from the other quantities
        ],
        //
        "reward_function": "grid_equivalent",
        // "economic_advantage", "net_profit"
        "use_synthetic_profile": "Building_17"
      },
      "load": {
        "scale": 1
      },
      // do not change, the load will stay fixed at scale 1
      "generation": {
        "scale": 1
      },
      // do not change, the generation will stay fixed at scale 1
      "storage": {
        // (optional) Defines the storage system to be attached to a participant
        "type": "Bess",
        // storage type, must exist in _devices
        "capacity": 5000,
        // Usable capacity defined in Wh
        "power": 3000,
        // constant charge/discharge rate defined in W
        "efficiency": 0.95,
        // one way efficiency. round-trip efficiency is efficiency^2
        "monthly_sdr": 0.05
        // monthly self-discharge rate as a fraction of usable capacity
      }
    }
    }
  }
  